- clean: ['~', '~/.config']
- shell:
    - mkdir -p ~/.config
- link:
    ~/.dotfiles:         dotfiles-example/
    ~/.i3:         .i3/
    ~/.confg:         config/
- shell:
    - ./.dotfiles/script/bootstrap
