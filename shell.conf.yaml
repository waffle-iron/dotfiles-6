- shell:
  -
    description: "Installing powerpill for faster downloads"
    command: "sudo pacman -S --noconfirm powerpill"
- shell:
  -
    description: "Bootstrap shell by carl0s"
    command: "$HOME/.dotfiles/script/bootstrap"
    stdout: true
    stderr: true
    stdin: true
